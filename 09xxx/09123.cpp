#include <iostream>
#include <string>
using namespace std;

void solve(void) {
  string a, b;
  cin >> a >> b;

  cout << b << a;
}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  solve();
  return 0;
}
